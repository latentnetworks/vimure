parse_graph_from_edgelist <- function(el, nodes, directed=T){
  vimureP$io$parse_graph_from_edgelist(el, nodes, is_undirected=directed)
}

py_to_r.sktensor.sptensor.sptensor <- function(x){
  x$toarray()
}

